## Run test
> $ node dev/test.js

One block has:
+ index (block number)
+ timestamp
+ pending transactions (wait for verify). It will be pushed into next new block
+ nonce (proof)
+ hash
+ previous block's hash
